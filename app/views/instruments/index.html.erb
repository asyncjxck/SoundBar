<h2>Instruments</h2>
<%= link_to("Create New Instrument", new_instrument_url) %>
<h3>List of Instruments</h3>

<table class='instruments-table'>
  <tr id="headers">
    <th><%= sortable "name" %></th>    
    <th><%= sortable "instrument_type" %></th>
    <th><%= sortable "price" %></th>
    <th>Description</th>
    <th>Brand</th>
    <th><%= sortable "created_at", "Date Listed" %></th>
  </tr>
  <% @instruments.each do |instrument| %>
    <tr id="each-table">
      <td><%= link_to instrument.name, instrument_path(instrument) %></td>
      <td><%= instrument.instrument_type %></td>
      <td><%= instrument.price %></td>
      <td><%= instrument.description %></td>
      <td><%= link_to instrument.brand.name, brand_path(instrument.brand) %></td>
      <td><%= instrument.created_at.strftime("%b %-d, %Y at %I:%M:%S %Z") %><td>
    </tr>
  <% end %>
</table>

